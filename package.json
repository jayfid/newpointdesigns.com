{
  "name": "newpointdesigns.com",
  "private": true,
  "engines": {
    "node": ">=0.12.0"
  },
  "devDependencies": {
    "ejs": "^3.1.6",
    "eslint": "^7.32.0",
    "express": "^4.17.1",
    "express-minify": "^1.0.0",
    "express-minify-html": "^0.12.0",
    "html-minifier": "^3.5.7",
    "node-sass": "^4.14.1",
    "node-sass-middleware": "^0.11.0"
  },
  "scripts": {
    "clean": "rm -rf node_modules dist .sass-cache",
    "start": "node ./app/server.js",
    "pretty": "npx prettier --write .",
    "lint": "npx eslint app",
    "build": "rm -rf dist && mkdir -p dist/images dist/scripts dist/styles"
  },
  "eslintConfig": {
    "env": {
      "es6": true,
      "node": true,
      "browser": true,
      "jquery": true
    },
    "extends": [
      "eslint:recommended"
    ]
  }
}
